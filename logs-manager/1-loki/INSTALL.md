# Loki

Base de datos de logs

## Instrucciones de despliegue

Crear el archivo `.env` y reemplazar los valores de ser necesario.

```bash
cp .env.sample .env
```

Ahora si, podemos desplegar el servicio:

```bash
docker compose up -d
```
